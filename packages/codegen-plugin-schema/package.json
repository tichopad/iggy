{
  "name": "@iggy/codegen-plugin-schema",
  "version": "0.0.0",
  "packageManager": "yarn@3.0.1",
  "main": "dist/plugin.js",
  "private": true,
  "scripts": {
    "build": "yarn run clean && yarn run compile",
    "clean": "rm -rf ./dist",
    "compile": "npx esbuild src/plugin.ts --outdir=dist --tsconfig=tsconfig.build.json --format=cjs --minify --sourcemap",
    "start": "yarn run build --watch",
    "test": "jest"
  },
  "peerDependencies": {
    "graphql": "^15.5.3"
  }
}
